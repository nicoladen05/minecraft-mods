#!/usr/bin/env bash
set -e
if [[ ! -d "/home/nico/dev/minecraft-mods" ]]; then
  echo "Cannot find source directory; Did you move it?"
  echo "(Looking for "/home/nico/dev/minecraft-mods")"
  echo 'Cannot force reload with this script - use "direnv reload" manually and then try again'
  exit 1
fi

# rebuild the cache forcefully
_nix_direnv_force_reload=1 direnv exec "/home/nico/dev/minecraft-mods" true

# Update the mtime for .envrc.
# This will cause direnv to reload again - but without re-building.
touch "/home/nico/dev/minecraft-mods/.envrc"

# Also update the timestamp of whatever profile_rc we have.
# This makes sure that we know we are up to date.
touch -r "/home/nico/dev/minecraft-mods/.envrc" "/home/nico/dev/minecraft-mods/.direnv"/*.rc
